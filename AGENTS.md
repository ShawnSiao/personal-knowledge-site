# 公开知识站工作规则

## 仓库职责

本仓库只保存公开网站代码和已经通过审核的公开内容。私有来源、项目清单、审核记录和本地路径保存在独立的私有知识库中。

## 内容来源

- `content/` 中的知识文章必须由私有知识库的白名单导出脚本生成。
- 首页和文章不得包含本地绝对路径、密钥、Token、私人联系方式、账号信息或未公开商业资料。
- 不得从其他项目直接复制原始代码、工作记录、第三方课程正文或受限素材。
- 修改公开内容前，先在私有知识库更新来源和审核状态，再重新导出。

## 构建与发布

- 本地构建使用 `npm ci`、`npx quartz plugin install` 和 `npx quartz build`。
- GitHub Pages 只从 `main` 分支的 GitHub Actions 构建产物发布。
- `public/`、`node_modules/` 和 `.quartz/` 是可重建产物，不提交到 Git。
- 发布前检查站内链接、生成页面和依赖审计结果。

## Git 规则

- 修改前检查当前分支、HEAD 和工作树。
- 只暂存当前任务相关文件，并在提交前检查暂存差异。
- 不强制推送，不用远端内容覆盖未提交的本地修改。
